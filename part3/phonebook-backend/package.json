{
  "name": "phonebook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "rm -rf build && cd ../phonebook-frontend && npm run build && cp -r build ../phonebook-backend",
    "deploy": "git push heroku master && heroku open",
    "deploy:full": "npm run build:ui && git add . && git commit -m \"build in front-end\" && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1",
    "logs:prod": "heroku logs --tail"
  },
  "author": "Tsigabu Mebrahtu",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "cors": "^2.8.5",
    "mongoose": "^5.8.9"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
